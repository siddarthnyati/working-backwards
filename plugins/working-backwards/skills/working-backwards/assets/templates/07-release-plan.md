# Release plan

Vertical slices. Each independently shippable and independently testable.
Rules: `references/release-decomposition.md`.

## Slices

```
R1 · <title>
Requirements: REQ-<ids>
Depends on: <R ids, or —>
Blocked by: <BLK ids, or —>
Ships: <what is true in production after this and not before>
Independently testable: yes | no — <if no, it is not a slice; merge or re-cut>
Test harness:
  - <test>
Rests on assumption: <REQ — which assumption, which question>
```

## Dependency DAG

```
R1 ──▶ R2 ──▶ R3
        │
        └───▶ R4 [BLOCKED BY BLK-<n>]
```

Edge list:
```
R1 -> R2
R2 -> R3
R2 -> R4
```

Checks: acyclic · every requirement in exactly one slice · orphans listed below.

**Orphan requirements (in no slice):** <ids, or none>
**Duplicated requirements (in more than one slice):** <ids, or none>

## Not schedulable
| Slice | Blocked by | Owner role | What unblocks it |
|---|---|---|---|

## Export
`jira-import.csv` generated by `scripts/export_jira.py`.
Epic rows precede their stories. `Source:` lines are preserved verbatim in Description —
traceability has to survive the export or it dies at the import.
